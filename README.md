## 機能
スキャンしたPDFの整形を目的としたソフトウェア。
- ページの傾きを修整する。
- ページの指定領域を全ページで同じ位置（ページ中央）に揃える。
- 汚れをペンで塗りつぶす。
- 編集後の状態をPDFとしてエクスポートする。

## 使用方法
必要なパッケージをインストールして実行するだけです。
### 必要なパッケージのインストール
```
pip install PyQt6 PyMuPDF
```

「PDFを開く」ボタンからPDFを読み込むことで編集可能になります。編集が終わったら、「PDFをエクスポート」ボタンから保存してください。

